{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "jsx": "preserve",
    "types": ["vite/client"],
    "moduleResolution": "bundler",
    // TODO: Remove when base config will be stricter.
    "strictNullChecks": true
  }
}
